# NexaVault Website

机构级数字资产基础设施官网，基于 React + TypeScript + Vite 构建，并参考 `ustarpay-website` 的 i18n、SEO 与页面拆分方式。

## Scripts

```bash
npm install
npm run dev
npm run typecheck
npm run build
```

开发服务器默认运行在 `http://localhost:5174`。
